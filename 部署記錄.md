# 部署記錄 - 添心生產力助手 (Client)

## 2026-02-26 15:10 (v1.10.2)
- **修改摘要**:
  - [功能] 打卡定位優化：新增「台南店/高雄店」座標備援。若 IP 定位超過 5 秒未回應，自動套用所屬分店座標，徹底解決「網路異常」提示。
  - [修復] 徹底修復 `tray.js` 詳細統計視窗中的「個人待辦清單」渲染邏輯，解決 ✅、↩️、🗑️ 按鈕無反應的問題。
  - [優化] `config.js`：導入 `STORE_LOCATIONS` 全域定義，方便未來擴充。
- **修改檔案**: `config.js`, `checkinService.js`, `tray.js`
- **結果**: 成功發布 v1.10.2，GitHub Tag 已推送。


## 2026-02-26 11:55 (v1.10.1)
- **修改摘要**:
  - [修復] 修正 `config.js` 的 API 網址，恢復後端正確的打卡緩存判定邏輯。
  - [修復] 修正 `tray.js` 的詳細統計視窗中，個人待辦清單 (To-do) 無法正確顯示與新增的 UI 渲染 Bug。
  - [優化] `checkinService.js` 打卡流程：增加 IP 定位超時控制 (2秒)，避免網路波動導致打卡顯示「網路異常」。
- **修改檔案**: `config.js`, `tray.js`, `checkinService.js`
- **結果**: 成功發布 v1.10.1，程式碼已推送並標記 GitHub Tag。


## 2026-02-26 11:45 (v1.10.0)
- **修改摘要**:
  - [功能] 實作 iCloud 行事曆訂閱 (ICS 解析)，自動同步提醒事項。
  - [功能] 桌機免 LIFF 快速打卡功能。
  - [功能] 個人待辦清單 (To-do List) 本地化儲存。
  - [優化] 解決了 Git 指令鎖定導致的部署中斷問題。
- **修改檔案**: `checkinService.js`, `tray.js`, `storage.js`, `reminderService.js`
- **結果**: 成功發布 v1.10.0，GitHub Actions 編譯中。



## v1.11.1 - 2026-02-26 15:30
### 升級摘要
- **私人助理小秘書系統**：新增「可愛小秘書」動畫 UI，提升使用者互動親切感。
- **進階提醒功能**：
    - **提前提醒**：支援 5/10/30 分鐘提前通知。
    - **重複提醒**：支援「每天」或「每週」自動重複任務。
    - **自動狀態重置**：每日凌晨自動重置重複任務的提醒狀態。
- **介面優化**：重構 Tray 詳細統計視窗，補全日期與時間輸入麵板。

## 2026-02-26 11:30 (v1.10.0)
- **修改摘要**:
  - [新增] 桌機免 LIFF 一鍵打卡功能
  - [新增] iCloud 行事曆唯讀同步 (ICS解析)
  - [新增] 本機個人待辦清單
- **修改檔案**: tray.js, checkinService.js 等
- **結果**: 部署 v1.10.0 Release 版

## 2026-02-26 09:41 (v1.9.1 Tag Rebuild)
- **修改內容**: 
  - [修復] 發現前次的 Github Actions 編譯包無效。原因為前次推送時受限於 Windows `index.lock` 檔案卡死，造成代碼 Commit 失敗，但指令串列中的 `git tag v1.9.1` 持續執行並貼附在舊的 `v1.9.0` 節點上。
  - [機制] 已強制移除 `.git/index.lock`，並重新將 `tray.js` 的修正包裹成功提交(`fix: stop 1-min interval API request`)。
- **修改檔案**: `(無修改代碼，純流程修正)`
- **結果**: 重新撤銷並推播 `v1.9.1` 標籤，成功觸發最新且正確的 CI/CD 發布程序。


## 2026-02-26 09:25 (v1.9.1 Hotfix)
- **修改內容**: 
  - [修復] 緊急修復詳細統計視窗的背景 `1 分鐘` 自動刷新 UI 時，會無差別觸發 `checkinService.refreshWorkInfo()` 的嚴重 Bug，這導致先前實作的「打卡即停(10分鐘冷卻)」完全失效，造成 GAS API 被高頻率擊碎。
  - [機制] 限定只有使用者手動點擊托盤 (`isManual=true`) 時才呼叫同步 API，其餘 UI 的靜默自動更新僅依賴本地 `storageService` 的時間累加。
- **修改檔案**: `tray.js`
- **結果**: 徹底消滅每 60 秒一次的不合理 API 請求，伺服器負載回歸平靜。


## 2026-02-26 09:15 (v1.9.0)
- **修改內容**: 
  - [功能] 深度掛接 LIFF 網頁打卡，支援托盤選單與統計視窗「📲 開啟網頁打卡」按鈕。
  - [優化] 實作「打卡即停」API 減載，大幅降低偵測到打卡後的後端輪詢頻率。
  - [核心] 修正跨天數據重置判定，確保 07:00 AM 前不誤清數據（跨日加班保護）。
  - [穩定性] AppCore 加入防自殺攔截，確保熱更新啟動 Shell 期間進程不被誤砍。
  - [隱私] 數據去識別化：移除小時分佈圖與網頁網址，僅保留應用排行。
- **修改檔案**: `checkinService.js`, `appCore.js`, `tray.js`, `reminderPreload.js`
- **結果**: 代碼已推送，Tag v1.9.0 已發布，GitHub Actions 編譯中。


### 2026-02-23 10:45 - 徹底根治視覺不一致與殘留視窗問題 (v1.7.7 Patch)
- **日期時間**: 2026-02-23 10:45
- **修改摘要**: 
  - **抑制舊版彈窗**: 熱更新無法覆蓋原生的 `main.js`，導致舊版的侵入式 `dialog` 依然會執行。改以在 `config.js` 的 `get('lastNotifiedPatch')` 強制偽裝返回最新的補丁版本號，成功騙過舊版主程式，徹底讓對話框噤聲。
  - **同步全域版本號**: 更新 `tray.js` 不再吃死最初安裝的版本 (`app.getVersion()`)，而是實作 `getEffectiveVersion()`，讓所有 UI (Tooltip 與統計視窗) 都能精確反映目前熱更新至第幾版。
  - **消滅幽靈視窗**: 在 `tray.js` 初始化時引入 `win.destroy()` 清除邏輯，如果系統存在多個舊的統計視窗，一律清理乾淨，不再產生「三個視窗」的弔詭狀態。
- **修改檔案**: 
  - `client/src/config.js`: 攔截 `lastNotifiedPatch` 設定讀取
  - `client/src/tray.js`: 引入即時全域版號機制，主動清除孤兒視窗
  - `client/package.json`: 升版 v1.7.7
- **結果**: 完美解決使用者提出的三個痛點，程式視覺與反饋將完全符合熱更新的狀態。

### 2026-02-23 09:30 - 修復核心體驗：打卡提醒雙重觸發與關閉失效問題 (v1.7.6 Patch)
- **日期時間**: 2026-02-23 09:30
- **修改摘要**: 
  - **雙重觸發修復**: 移除 `reminderService.js` 中重複建置的 5 秒開機提醒，統一交由迴圈中的 10 秒排程處理，解決了打卡提醒同時跳出兩個的問題。
  - **提醒關閉失效修復**: 修正 `reminderService.js` 中 IPC 關閉視窗的目標捕捉機制（由固定最後建立的變數改為直接抓取 `sender` 視窗），並修復 `tray.js` 產生詳細統計 HTML 內的 `SyntaxError`（遺漏右括號），徹底解決在各處點擊「✅ 完成」或「⏰ 稍後」會關不掉視窗的 Bug。
  - **干擾性通知移除**: 將 `main.js` 熱更新加載完成後的 `dialog.showMessageBox` 移除，改由無干擾的系統防呆通知或靜默套用，不再干擾使用者正常作業。
- **修改檔案**: 
  - `client/src/reminderService.js`: 修復提醒排程與 IPC 關閉邏輯
  - `client/src/tray.js`: 修復 `render` 邏輯與腳本的語法錯誤
  - `client/main.js`: 移除侵入式的更新完成彈窗
- **結果**: 提醒視窗數量恢復正常，且按鈕功能恢復可點擊關閉。自動更新將完全於幕後靜默生效。

### 2026-02-22 21:57 - 補齊：計畫書更名與各項狀態對齊 (Documentation Update)
- **日期時間**: 2026-02-22 21:57
- **修改摘要**: 
  - 因為前次 1.7.5 Git 推播時 2026-02-01_IMPLEMENTATION_PLAN.md 已經更名為 添心小助手計劃書.md 但未被加入版本控制中，重新手動修復 Git 狀態並更新目錄。
  - 將計畫書內原先預定的五大 Phase 狀態全部更新為「已完成」。
  - 更新了計畫書中的目錄結構以反映目前真實導入的 hotReloader.js、checkinService.js 等模組。
  - 更新 PATCH_SYSTEM_PLAN.md 標示為已實作並補上 Module._compile 的沙盒紀錄。
- **針對目錄**: 
  - SPEC/添心小助手計劃書.md (修改狀態與目錄架構)
  - SPEC/PATCH_SYSTEM_PLAN.md (更新實作狀態)
- **結果**: 所有的實作計畫書、熱更新文件狀態現在已經 100% 與客戶端最新的 v1.7.5 打卡防呆和沙盒熱更是同等進度，並已推播。

### 2026-02-22 21:07 - 實用優化：修正無限更新通知、全自動打卡完成判定 (v1.7.5 Patch)
- **日期時間**: 2026-02-22 21:07
- **修改摘要**: 修補熱更新模組因版本錯誤判斷而無限重拉補丁的問題，以及修正因通知重複出現干擾使用者的痛點。另外實作了系統自動判別打卡、全自動取消未打卡提醒的便利功能。將紀錄檔案正名為「添心小助手計劃書.md」。
- **修改檔案**: 
  - client/src/updater.js: 新增檢測 patch_version.json 避免重複下載
  - client/main.js: (1) 補丁成功提示僅針對新版。(2) 增加自動判別打卡紀錄的功能，一旦發現已打卡自動將提醒標記成完成，並關閉視窗。
  - SPEC/2026-02-01_IMPLEMENTATION_PLAN.md: 更名為「添心小助手計劃書.md」，並更新內容與紀錄。
  - client/package.json & tray.js: 版本號與字眼變更為 v1.7.5。
- **結果**: 邏輯優化已經完成，稍後自動拉取 1.7.5 補丁時，以上優化將自動生效！

### 2026-02-22 20:06 - 正式無損更新測試 (v1.7.4 Patch)
- **日期時間**: 2026-02-22 20:06
- **修改摘要**: 移除上一版本測試用的惡意當機代碼，並在托盤選單的標題加上「[熱更新 v1.7.4]」的字眼，用以證明能透過 Patch 覆蓋 UI 層的顯示文字且正常運作。
- **修改檔案**: 
  - client/src/monitor.js: 移除測試用的 throw new Error()
  - client/src/tray.js: 修改標題字串
  - client/package.json: 版本號變更為 1.7.4
- **結果**: 已發布 v1.7.4 更新包。測試客戶端只要無崩潰地接收並加載顯示了新的文字，即代表整個「動態修補與掛載策略」完全成功。

### 2026-02-22 18:05 - 故意損壞的補丁測試 Round 2 (v1.7.3 Patch Test)
- **日期時間**: 2026-02-22 18:05
- **修改摘要**: 推送故意損壞補丁 v1.7.3，進行正式環境之熔斷壓力測試與驗證紀錄。
- **修改檔案**: 
  - client/src/monitor.js: 再次加入 throw new Error()
  - client/package.json: 版本號變更為 1.7.3
- **結果**: 測試客戶端 (1.7.2) 將會在 15 分鐘內自動偵測、下載、嘗試加載，並觸發熔斷將 .corrupted_* 隔離，最終無縫回退至原始健康代碼繼續運行。為一小時後的 1.7.4 正式升級暖身。

### 2026-02-22 17:34
*   **指令摘要**: 深入重構 hotReloader 修正致命的底層路徑解析漏洞以發布 v1.7.2
*   **執行摘要**: 
    - 發現 V1.7.0 本身有著嚴重的結構性缺陷：從 \pp_patches\ (位於 AppData) 以一般 \
equire\ 引入套件時，會因為不在原本的程式目錄 (app.asar) 而徹底無法讀取 \
ode_modules\ 中的任何依賴如 \sql.js\, \ctive-win\。造成這只要一引入就會報錯並死當。
    - 捨棄一般 \
equire(patchFilePath)\。改為在 \hotReloader.js\ 中將檔案轉為字串，透過 Node 底層核心 \Module._compile\ 動態編譯並賦予該執行的沙盒一個「等同原廠目錄位置 (cwd)」的偽裝。
    - 釋出 v1.7.2 徹底解決了因增量補丁路徑與第三方套件 \
ode_modules\ 脫節而造成的死機問題。
*   **結果**: 程式完成深度修復，並推播了 \1.7.2\ 主版本以供徹底防呆更新安裝。

### 2026-02-22 16:15 - 故意損壞的補丁測試 (v1.7.1 Patch Test)
- **日期時間**: 2026-02-22 16:15
- **修改摘要**: 刻意在程式主流程加入報錯代碼，並封裝成新版補丁測試「增量熱更熔斷機制」。
- **修改檔案**: 
  - `client/src/monitor.js`: 加入 `throw new Error()`
  - `client/package.json`: 版本號變更為 1.7.1
- **結果**: 觸發打包上傳 `patch-v1.7.1.zip` 提供 1.7.0 版本客戶端下載並測試熔斷機制，且測試後已復原原始代碼。

### 2026-02-22 14:17
*   **修改摘要**: 實作增量熱更新 (Patching)、熔斷保護機制與活體替換
*   **修改檔案**: 
    - client/src/hotReloader.js (新增，負責動態引入與熔斷)
    - client/src/updater.js (新增，負責偵測與下載 Github patch zip)
    - client/main.js (改用 hotReloader、實作 
restartAppServices 免關閉熱更新機制)
    - client/.github/workflows/build.yml (增加打包 patch zip 步驟)
    - client/package.json (升版 1.7.0，安裝 adm-zip)
*   **結果**: 熱更新功能已經整合至 v1.7.0。支援程式錯誤熔斷和無損體驗的熱抽換架構。

### 2026-02-22 13:30 - 緊急修復：管理中心 UI 渲染異常 (v1.6.5)
- **日期時間**: 2026-02-22 13:30
- **修改摘要**: 
  - **修復語法錯誤**: 解決了 `adminDashboard.js` 中因模板字面量嵌套產生的 `SyntaxError`。
  - **優化渲染邏輯**: 將表格生成改為不具衝突的單引號字串拼接，並對日誌內容中的特殊反引號進行十六進位轉義。
  - **URL 修復**: 修正 `loadURL` 中錯誤產生的空格問題。
- **修改檔案**: 
  - `src/adminDashboard.js`: 重構 `renderHistoryTable` 與載入邏輯。
- **結果**: 管理員報表中心已恢復顯示，不再彈出 JavaScript 錯誤視窗。

### 2026-02-22 10:00 - 機制大幅優化：靜默熱更新與自動版本發布 (v1.6.3)
- **日期時間**: 2026-02-22 10:00
- **修改摘要**: 
  - **靜默更新**: 修改 `main.js` 中的 Updater 邏輯。現在下載新版後**不再跳出彈窗**干擾使用者，而是靜默準備好。使用者下次開啟程式或重啟系統時會自動完成更新。
  - **自動發布**: 在 `package.json` 中加入 `releaseType: "release"`。現在 GitHub Actions 編譯完會**直接發布**，不再停留在開發者才能看見的 Draft 狀態。
- **修改檔案**: 
  - `main.js`: 移除 `update-downloaded` 的 GUI 對話框。
  - `package.json`: 新增發布類別與升級版本號。
- **結果**: 達成了「無感更新」的技術門檻，並解決了更新發布需要人工手動點選 GitHub Release 的延遲問題。

### 2026-02-22 09:35 - 緊急部署：小助手 v1.6.2 (CI/CD 觸發修正)
- **日期時間**: 2026-02-22 09:35
- **修改摘要**: 
  - **解決標籤未觸發**: 修改 `.github/workflows/build.yml` 加入版本註解，強制 Git 偵測實質變動。
  - **版本遞增**: 由於雲端 Release 緩存與標籤衝突，將版本號跳過 `.1` 直接升級至 `v1.6.2`。
- **修改檔案**: 
  - `.github/workflows/build.yml`: 加入版本戳記。
  - `package.json`: 升級版本號至 1.6.2。
- **結果**: 成功在 GitHub Actions 觸發 Build #3 任務，正在生成安裝檔。

### 2026-02-22 09:15 - 後端修復：恢復 GAS 常數定義
- **日期時間**: 2026-02-22 09:15
- **修改摘要**: 
  - 修正 GAS 後端報錯 `CONST is not defined`。
  - 重建 `Constants.gs` (Constants.js) 將棄用的常數恢復至主目錄。
- **修改檔案**: 
  - `backend/CheckinSystem/Constants.js`: 恢復 `SHEET` 與 `CACHE` 常數物件。
- **結果**: 恢復後端打卡記錄寫入、通知發送與每日自動盤點功能。

### 2026-02-22 08:50 - 核心優化：實作本地記憶與持久化恢復機制
- **日期時間**: 2026-02-22 08:50
- **修改摘要**: 
  - **數據恢復**: `MonitorService` 啟動時會自動從資料庫恢復今日已累積的工作與休閒時間。
  - **狀態持久化**: `ReminderService` 記錄今日提醒的完成狀態（已完成/稍後），避免重啟後重複彈窗。
  - **存檔強化**: 將資料庫自動寫入頻率提升至 15 秒，並新增 `before-quit` 安全存檔機制。
- **修改檔案**: 
  - `src/storage.js`: 新增 `getTodayTotalSeconds`, 縮短 `saveTimeout`。
  - `src/monitor.js`: 新增 `_restoreTodayStats` 並於啟動時呼叫。
  - `src/reminderService.js`: 實作 `_saveTodayStatus` 與 `_loadTodayStatus`。
  - `main.js`: 監聽 `before-quit` 事件執行安全關閉。
- **結果**: 小助手具備了「記憶能力」，重啟後數據不歸零，且已完成的提醒不會再次出現。

### 2026-02-22 01:25 - 效能優化：移除心跳回報機制
- **日期時間**: 2026-02-22 01:25
- **修改摘要**: 
  - 徹底移除 Electron 客戶端向 GAS 後端發送的心跳回報（Heartbeat）。
  - 減少後端請求頻率，解決 GAS 配額過度消耗與日誌錯誤噪音問題。
- **修改檔案**: 
  - `main.js`: 移除 `monitorService.startHeartbeat()` 的呼叫。
  - `src/monitor.js`: 註解掉心跳發送的實作邏輯。
  - `src/adminDashboard.js`: 
    - 移除已失效的「即時監控」分頁。
    - 實作「點擊聚焦」功能：點擊表格列可將圓餅圖與數據卡片縮放至該單日/單人的數據。
    - 實作 Modal 視窗詳情查看細目。
- **結果**: 顯著減輕伺服器負擔，並提升管理員查閱歷史報表的直覺度與深度。
#   �r��  -   �m�_u"u�R�RKb  v 1 . 1 0 . 0 
 -   �egBf���
 -   �O9eXd����e�XteT;N�c�S�_wcu�0�[\O},g0WP�N�_���N�( C R U D ) 0�[\O  A p p l e   i C l o u d   ( I C S )   �UTTek㉐g��S2���
r͑j~0
 -   \Hh��m�_u"u�R�RKb  ( C l i e n t ) 
 -   Hr,g_��v 1 . 1 0 . 0 
 
 
### 2026-02-26 16:40
- **修改摘要**: 修復 client 資料夾 Git 屬性錯誤 (160000 轉 100644) 與檔案還原。
- **修改檔案**: client/package.json, client/src/*, .github/workflows/build.yml
- **結果**: 成功透過 Reflog 還原實體原始碼，並修正熱更新版本號至 v1.11.3。

### 2026-02-26 16:55
- **修改摘要**: 導入輕量化 (Lightweight) 打包流程。
- **執行內容**: 移除 npm install，改為直接打包 src，解決 socket hang up 問題。
- **結果**: v1.11.4 成功發布，熱更新通路全面恢復。
