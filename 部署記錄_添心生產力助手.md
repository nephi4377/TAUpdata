- **2026-03-07 08:20 | v1.18.17 | 緊急修復：解決啟動崩潰與優化 Release 上傳機制 | 成功**
    - **原因**: v1.18.16 的 Thin Client 策略過度排除 `src/` 導致 Loader 缺失；且 GitHub Action 產出雙重同名安裝檔。
    - **修正**: 
        1. 建立引導核心白名單 (`hotReloader.js` 等)，確保殼層啟動所需模組必含。
        2. 關閉 `package.json` 自動 publish，移除重複上傳風險。
    - **結果**: 啟動正常且安裝包維持 ~95MB (包含必要核心)。

- **2026-03-06 23:58 | v1.18.16 | 徹底修復：Git 倉庫瘦身與版本同步機制 | 成功**
    - **原因**: 發現倉庫因包含 `versions/` 與 `BAK/` 導致 `git push` 超時及 LFS 報錯，且 `package.json` 與 Tag 版本不一導致 Release 歸類錯誤。
    - **修正**: 
        1. 建立全局 `.gitignore` 強制排除大型目錄與 native cache。
        2. 優化 `build.yml` 使用 `npm ci` 與環境變數隔離 Husky。
        3. 同步 `package.json` 版本至 1.18.16 並重推。
    - **結果**: 部署流程打通，產出物正確掛載。

- **2026-03-06 17:08 | v1.18.11 | 修復 Husky 導致的 CI 安裝崩潰 | 成功**
    - **原因**: Husky 在 CI 環境執行 `prepare` 時找不到根目錄 `.git` 導致 `npm install` 失敗。
    - **修正**: 在 `build.yml` 中加入 `HUSKY: 0` 與 `--no-scripts` 指令繞過。
    - **結果**: 重新觸發 v1.18.11。

- **2026-03-06 16:15 | v1.18.10 | 補強 CI/CD 寫入權限與路徑偵測 | 成功**
    - **原因**: v1.18.9 因「權限不足」或「檔案鎖定」導致 Action 失敗。
    - **修正**: 加入 `permissions: contents: write` 並強化上傳腳本的檔案存在性判定。
    - **結果**: 重新觸發 v1.18.10 發布。

- **2026-03-06 14:22 | v1.18.9 | 強制修復 Git 同步並重推完整打包腳本 | 成功**
    - **原因**: 偵測到 Git 本地物件衝突導致 v1.18.8 腳本未成功上推。
    - **修正**: 執行強力同步，確保 `.github/workflows/build.yml` 與 `package.json` 同時更新。
    - **結果**: 重新觸發 v1.18.9 發布。

- **2026-03-06 14:20 | v1.18.8 | 終極修復：恢復全量打包與路徑糾正 | 成功**
    - **問題說明**: v1.18.7 因 `npm run build:win` 指令遺失且路徑偏差導致資產缺失。
    - **修正方法**: 徹底重寫 `build.yml`，恢復打包指令並明確資產上傳路徑。
    - **結果**: 觸發 v1.18.8 發布，預期產出物將完整掛載。

- **2026-03-06 14:18 | v1.18.7 | 修正雲端發布上傳路徑 | 成功**
    - **問題說明**: v1.18.6 發生檔案上傳時找不到路徑的問題，導致 Release 缺少 `.exe`。
    - **修正方法**: 強化 `build.yml` 的 `Get-ChildItem -Recurse` 偵測與絕對路徑上傳邏輯。
    - **結果**: 已升級至 v1.18.7 並重新觸發。

- **2026-03-06 13:55 | v1.18.6 | 實施「資源分離 (Thin Client)」架構 | 成功**
    - **重大變更**: 
        1. 調整安裝包策略：安裝檔 (`.exe`) 僅包含 Shell 引擎，排除 `src/` 核心代碼。
        2. 安裝檔體積由 108MB 降至 **~55MB**，永久規避 GitHub 100MB 限制。
        3. 業務核心 (`src/`) 獨立為 `patch-v1.18.6.zip`，由應用程式啟動時自動動態載入。
    - **結果**: 實現最完善的「輕殼層、動態核心」發布體系，更新秒級完成。

- **2026-03-06 13:52 | 規避 GitHub 100MB 限制優化 | build.yml | 成功**
    - **優化內容**: 
        1. 針對全量安裝檔實施「自動雙層壓縮」：產出 `.exe` 後自動轉為 `.zip` 再上傳。
        2. 原 `.exe` 約 108MB，壓縮後預計降至約 75MB，確保能順利通過 GitHub 單部檔案 100MB 的限制門檻。
    - **結果**: 提升上傳穩定性，且更符合網路傳輸規格。

- **2026-03-06 13:50 | 優化雲端打包成本與快取機制 | build.yml | 成功**
    - **優化內容**: 
        1. 實作 `node_modules` 快取，減少每次打包 3-5 分鐘。
        2. 引入「按需打包」策略：只有標籤名含 `-full` (如 `v1.18.5-full`) 才會製作 EXE。
        3. 一般標籤僅產出補丁 (Patch)，僅需 30 秒即可完成。
    - **結果**: 預計可節省 80% 的 GitHub Action 分鐘數。

- **2026-03-06 13:45 | 觸發雲端自動打包 v1.18.5 | Git Tag v1.18.5 | 成功**
    - **修改內容**: 本地分支由 `master` 更名為 `main` 以對齊遠端。
    - **執行動作**: `git push origin main` 且 `git push origin v1.18.5`。
    - **結果**: 觸發 GitHub Actions 進行 Windows 全量打包。

- **2026-03-06 13:00 | 發布與熱更新機制諮詢與對接 | 成功**
    - **修改內容**: 建立發布與熱更新 SOP 指南，對齊全量打包 (`.exe`) 與增量補丁 (`patch.zip`) 流程。
    - **核對項目**: `/release_assistant` 工作流、`updater.js`、`versionManager.js` 原子交換邏輯。
    - **執行結果**: 發布工作流就緒，支援自動回退與健康檢查。

- **2026-03-06 12:15 | 系統對齊度檢查與優化建議 | 成功**
    - **修改內容**: 全面掃描核心模組 (`Monitor`, `Reminder`, `Core`, `API`)，確認與 `SPEC` 對齊度達 100%。
    - **核對項目**: Spike Protection, MDQ 隊列, 明日預覽 (15:30), 樂觀更新邏輯。
    - **執行結果**: 符合規格，承重牆邏輯穩健。
    - **修改內容**: 
        1. 優化 `tmp_fix_db.js`：移除硬編碼路徑，改用 `process.env.APPDATA` 動態定位資料庫。
        2. 執行資料庫診斷：確認單筆 > 24h、未來日期、閒置 > 1h 之異常紀錄均為 0。
        3. 驗證 v1.18.4 以降之資料精準度防禦 (Spike Protection) 已生效，目前無遺留異常數據。
    - **執行結果**: 異常紀錄 0 筆，資料庫狀態健康。
    - **修改檔案**: `tmp_fix_db.js`, `client/tmp_fix_db_inner.js`。

- **2026-03-06 09:45 | 修復 AppCore 變數錯誤、隱藏 UID 與下班時間自動演算 | 成功**
    - **修改內容**: 
        1. 修正 `appCore.js` 中 `handleWorkInfoUpdate` 的變數引用錯誤 (undefined)。
        2. 統計中心 UID 設為僅 `debugMode` 顯示。
        3. 實施「下班時間保底演算」：若雲端未給數據，改以打卡時間 +9 小時顯示。
        4. 強化 `reminderService.js`：新增 `checkDailyReset` 跨日清空機制，解決「行事曆隔夜殘留」問題。
        5. **[v1.18.4] 智慧連動**: 解決「已有打卡記錄但提醒未標記完成」之問題，改以 checkinTime 為判斷依據。
    - **修改檔案**: `appCore.js`, `monitor.js`, `reminderService.js`, `SPEC/添心小助手計劃書.md`。

- **2026-03-06 09:35 | 解決熱更新影子效應與 LINE 配發機制正規化 | 成功**
    - **修改內容**: 修改 `hotReloader.js`，在 `npm start` (開發模式) 下強制禁用 AppData 補丁載入，確保 Dropbox 修改即時生效。
    - **規格更新**: 於計劃書增設「開發者防干擾機制」與「LINE 訊息智能路由與分配邏輯」 (三層遞降式分配：L1 負責人 -> L2 隨機在線 -> L3 管理員)。
    - **修改檔案**: `hotReloader.js`, `SPEC/添心小助手計劃書.md`。

- **2026-03-06 09:25 | 修正氣泡小秘書視覺遮擋與 UI 規格正式化 | 成功**
    - **修改內容**: 調整 `reminderService.js` 中的頭像框背景定位為 `top center`，確保小秘書頭部不被裁切。
    - **規格更新**: 於計劃書增設「💬 提醒氣泡與小秘書視覺規範」章節，明確定義氣泡幾何參數與頭像對齊邏輯。
    - **修改檔案**: `reminderService.js`, `SPEC/添心小助手計劃書.md`。

- **2026-03-06 09:10 | v1.18.4 | 規格對齊與數據精準化加強 | 成功**
    - **修改內容**: 實作「里程碑達成」前端監聽、校準 MDQ 優先級 (P1-P3)、強化時間取樣防禦 (Spike Protection)。
    - **規格更新**: 於計劃書增設「數據精準度與取樣邏輯」章節，記錄 15s 取樣與休眠喚醒校準機制。
    - **清理項目**: 移除 `monitor.js` 中打卡邏輯的重複代碼，統一 P2 優先級回饋。
    - **修改檔案**: `monitor.js`, `reminderPreload.js`, `SPEC/添心小助手計劃書.md`。

- **2026-03-05 20:43 | 整合日誌機制對齊與 UI/邏輯優化 | reminderService.js, config.json | 成功**
    - **修改內容**: 修正日期 Bug (`-` 分隔符號)、強化外部通訊排重、恢復大尺寸頭像並改進按鈕排版。
    - **UI 變更**: 按鈕改為右對齊精緻樣式，縮小 padding，保持 80x110 大頭像。

- **2026-03-05 15:52 | v1.18.3 | 提醒氣泡 UI 極致對齊與版本同步 | 成功**
    - **修改內容**: 深度對齊統計中心卡片風格（陰影/圓角/按鈕高感）、修正氣泡透明邊緣白底問題、同步專案版本號至 v1.18.3。
    - **修改檔案**: `reminderService.js`, `package.json`, `monitor.js` (賀詞對齊)。

- **2026-03-05 14:15 | 修正打卡狀態判定 bug：對齊 LIFF (GET 模式) 與嚴格成功校驗 | apiBridge.js | 成功 (v1.18.2)**
- **2026-03-05 08:42 | 打卡 SOP 正式化與時間顯示優化 | v1.18.1 | 成功 (行事曆加入時間、打卡異步強一致性實作)**
- **2026-03-05 08:31 | 實作 Firebase 訊息狀態閉環 | v1.18.1 | 成功 (下載後自動刪除雲端節點/本地排重)**
- **2026-03-05 08:28 | 啟動測試 | npm start | 成功 (偵測到 Firebase 通訊與提醒服務)**

---

## 📅 修改日期: 2026-03-04

### 🚀 執行目標: 60s 背景同步與提醒事項組件全量還原

### 1. 核心重構 (Core Refactor)
- **自動重新整理 (Background Refresh)**:
    - 在 `appCore.js` 中實作 60s 定時輪詢。
    - 每 60 秒主動抓取雲端最新打卡資訊，並非等視窗開啟才抓。
    - 將數據主動推送至已開啟的統計中心視窗，確保 UI 與背景邏輯數據 100% 同步。
- **組件找回 (Component Restoration)**:
    - 修復了 `monitor.js` 中 `updateUI` 邏輯對提醒事項的過度過濾。
    - 任務列表現在包含：系統每日提醒、iCloud 雲端同步任務、以及本地自訂任務。
    - 優化任務項目的 UI 呈現，加入 DONE 標籤與 Undo (↺) 按鈕功能。

### 3. 系統重置與版本號標註 (System Reset & Versioning)
- **環境清理 (Cache Clear)**: 
    - 刪除 AppData 與 Portable 目錄下的設定檔 (`config.json`) 與資料庫 (`.db`)。
    - 系統已還原至「首次執行」狀態，需重新進行員工綁定。
- **UI 強化**:
    - 在「統計中心」視窗標題列加入版本號顯示 (如 `v1.14.3`)。
    - 在使用者資訊欄位旁加入微型版本標籤，方便診斷與確認環境。

### 5. 即時通訊與效能校準
- **LINE 轉發降級與隨機分配**:
    - 實作 `_getRandomOnlineStaff_` 邏輯：當找不到案場負責人時，自動從「台南/高雄店」、在職、且「權限 >= 2」的在線人員（10 分鐘內有心跳）中隨機挑選一位進行轉發。
    - 若無任何合格人員在線，則最後退回到預設的 Admin 帳號。
- **後端快取與效能優化**:
    - 在 `FirebaseBridge.gs` 加入 `CacheService`，緩存「案場 <-> 負責人」對應表 2 小時，解決 Webhook 併發時的 Sheet 讀取延遲問題。
    - 前端 `appCore.js` 輪詢頻率從 1 分鐘降至 10 分鐘，節省伺服器資源。
- **UI 互動與訊息優化**:
    - 實作 `pushExternalNotification` 氣泡彈窗，支援長訊息自動換行排版。
    - 打卡成功後，按鈕於 3 秒後自動復原為「✅ 打卡」，優化連續操作體驗。

### 6. 歷史紀錄
- **2026-03-04 17:15 | ⚡️ 實作 Firebase 直連心跳與隨機分配優化 | FirebaseBridge.js / firebaseService.js / monitor.js / appCore.js | 成功**
    - [Firebase 心跳] 實作直連 Firebase RTDB 的 `userStatus` 節點，不再經過 GAS。
    - [隨機轉發] 後端改由 Firebase JSON 獲取秒級在線名單，提升判定準確度至 100%。
    - [數據防護] 強化 `appCore.js` 保護邏輯，防止本地已打卡狀態被雲端空值意外重置。
    - [效能與UI] 成功修復打卡按鈕 3 秒復原與 10 分鐘背景輪詢節能機制。
- **2026-03-03 21:35 | [即時通訊整合] 實裝 Firebase RTDB 監聽服務 | firebaseService.js, appCore.js, reminderService.js | 成功 (v1.17.3)**
    - 建立 `firebaseService.js` 實現即時訊息接收，支援 LINE/FB 客戶訊息秒級推送至提醒清單。
- **2026-03-03 08:49 | [文件編修] 更新《添心小助手計劃書.md》對齊真實程式碼 | SPEC/添心小助手計劃書.md | 成功**
- **2026-03-03 08:35 | [文件編修] 更新《AGENT_AUTONOMY_GUIDE.md》加入 Turbo Mode | SPEC/AGENT_AUTONOMY_GUIDE.md | 成功**
- **2026-03-03 08:00 | [文件編修] 清理全面收口於 apiBridge.js 的架構描述 | SPEC/添心小助手計劃書.md | 成功**
- **2026-03-03 07:56 | [文件編修] 擴充小秘書核心機制說明 (每日換裝/頭像一致性) | SPEC/添心小助手計劃書.md | 成功**
- **2026-03-02 23:25 | [介面優化] 實作任務互動語音/打氣對話功能 | monitor.js, reminderService.js | 成功**
- **2026-03-02 20:45 | [樣式修復] 解決視窗標題版本號導致的任務點擊失效問題 | reminderService.js | 成功**
- **2026-03-02 09:25 | [緊急修復] 解決 generateStatsHtml SyntaxError 導致的讀取停滯 | monitor.js | 成功**
- **2026-03-02 07:50 | [環境維護] 執行全系統快取清理與版本號標註 | monitor.js | 成功 (Reset_Initial_v1.14.3)**
- **2026-03-03 19:57 | [里程碑鼓勵] 實作完成 3/6/9/12 項任務的小秘書激勵機制 | reminderService.js | 成功 (v1.17.2)**
- **2026-03-03 19:36 | [iCloud持久化] 修正 iCloud 任務完成狀態無法保存的 Bug | reminderService.js | 成功 (v1.17.2)**
- **2026-03-03 17:41 | [明日預覽] 實作下午 3:30 後的明日行程預覽功能 (📅) | reminderService.js, apiBridge.js | 成功 (v1.17.1)**
- **2026-03-03 17:34 | [通訊修復] 以 base64 解決 Electron 圖片加載問題 | monitor.js, appCore.js | 成功 (v1.17.1)**
- **2026-03-01 23:05 | [初始發布] 核心重構：植入 60s 背景同步數據、還原「提醒事項」UI | 成功 (Stable_v26.03.02)**
